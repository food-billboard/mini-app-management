import React, { memo, FC, useMemo } from 'react'
import {  } from 'antd'
import { fabric } from 'fabric'
import { IBaseProps } from '../types'

interface IProps extends IBaseProps {
  
}

const Background: FC<IProps> = () => {

  return (
    <div>1111</div>
  )

}

export default memo(Background)